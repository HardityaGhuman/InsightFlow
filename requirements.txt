# InsightFlow dependencies
#
# Data generation and loading use the Python standard library only
# (random, csv, datetime) plus the psql CLI for COPY. This keeps setup
# dependency-free and reproducible.
#
# The single external dependency is litellm, used only by
# scripts/generate_ai_brief.py to send the SQL-calculated metrics JSON
# to an LLM (Groq, openai/gpt-oss-120b) and get back a stakeholder brief.
# Requires GROQ_API_KEY in the environment.

litellm>=1.55.0
